pre-commit:
  parallel: true
  files: git diff --staged --name-only
  commands:
    check:astro:
      run: pnpm check:astro
    check:biome:
      run: pnpm check:biome
    test:
      run: pnpm test run
    test:e2e:
      run: pnpm test:e2e
