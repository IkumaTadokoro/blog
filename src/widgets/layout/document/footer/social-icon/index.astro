---
import GitHub from "./icons/GitHub.svg";
import SizuMe from "./icons/SizuMe.svg";
import SpeakerDeck from "./icons/SpeakerDeck.svg";
import X from "./icons/X.svg";
import Zenn from "./icons/Zenn.svg";

import { SOCIAL_ACCOUNT } from "../../../../../shared/config/site";
import StyledIcon from "../../../../../shared/ui/styled-icon/index.astro";

type SocialAccount = keyof typeof SOCIAL_ACCOUNT;

interface Props {
  serviceName: SocialAccount;
}
const { serviceName } = Astro.props;
const serviceUrl = SOCIAL_ACCOUNT[serviceName];

const IconComponent = {
  GitHub,
  X,
  Zenn,
  SpeakerDeck,
  SizuMe,
}[serviceName];
---

<a href={serviceUrl}>
  <StyledIcon>
    <IconComponent />
  </StyledIcon>
</a>
