---
import type { BlogEntry } from "../../../entities/blog/model/schema";
import BlogCard from "../../../entities/blog/ui/blog-card/index.astro";

interface Props {
  page: Array<BlogEntry>;
}

const { page } = Astro.props;
---

<ul class="grid place-self-start gap-y-8 lg:gap-y-20 w-full">
  {
    page.map((blog) => (
      <li>
        <BlogCard blog={blog} />
      </li>
    ))
  }
</ul>

<style>
  ul::after {
    border-radius: 1rem;
    content: "";
    position: absolute;
    background: hsla(0, 0%, 77%, 0.05);
    pointer-events: none;
    z-index: 0;
    position-anchor: var(--anchor);
    inset: calc(anchor(top) - 20px) calc(anchor(right) - 20px)
      calc(anchor(bottom) - 20px) calc(anchor(left) - 20px);
    transition: inset 0.2s;
  }

  ul:has(li:hover) {
    --anchor: --active;
    --active: 1;
  }
  li:hover {
    anchor-name: --active;
    --li-active: 1;
  }
</style>
