---
import { cn } from "../utils/cn";

type Props = {
  to: string;
  className?: string;
  tabindex?: string;
};

const { to, className } = Astro.props as Props;
---

<a href={to} class={cn(className)}>
  <slot />
</a>
