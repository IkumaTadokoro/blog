---
import Github from "./icons/Github.svg";
import SizuMe from "./icons/SizuMe.svg";
import SpeakerDeck from "./icons/SpeakerDeck.svg";
import X from "./icons/X.svg";
import Zenn from "./icons/Zenn.svg";

export type SocialIcon = "Github" | "X" | "Zenn" | "SpeakerDeck" | "SizuMe";

const socials: Record<SocialIcon, { href: string; linkTitle: string }> = {
  Github: {
    href: "https://github.com/IkumaTadokoro",
    linkTitle: "GitHub",
  },
  X: {
    href: "https://x.com/ikumatdkr",
    linkTitle: "X",
  },
  Zenn: {
    href: "https://zenn.dev/ikuma",
    linkTitle: "Zenn",
  },
  SpeakerDeck: {
    href: "https://speakerdeck.com/ikumatadokoro",
    linkTitle: "SpeakerDeck",
  },
  SizuMe: {
    href: "https://sizu.me/ikuma",
    linkTitle: "しずかなインターネット",
  },
};

interface Props {
  icon: SocialIcon;
}

const { icon } = Astro.props;
---

<a
  href={socials[icon].href}
  class="w-6 aspect-square hover:opacity-70 transition-opacity rounded-md"
  aria-label={socials[icon].linkTitle}
>
  {
    (() => {
      switch (icon) {
        case "Github":
          return <Github class="fill-gray-300 w-full h-full" />;
        case "X":
          return <X class="fill-gray-300 w-full h-full" />;
        case "Zenn":
          return <Zenn class="fill-gray-300 w-full h-full" />;
        case "SpeakerDeck":
          return <SpeakerDeck class="fill-gray-300 w-full h-full" />;
        case "SizuMe":
          return <SizuMe class="fill-gray-300 w-full h-full" />;
        default:
          return null;
      }
    })()
  }
</a>
